import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fr.dreamycode.grandma',
  appName: 'grandma',
  webDir: 'dist/grandma/browser'
};

export default config;
